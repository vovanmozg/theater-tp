version: '3'
services:

  api:
    build: ./api
    command: bash -c "rm -f tmp/pids/server.pid && rake db:migrate && bundle exec rails s -p 3001 -b '0.0.0.0'"
    volumes:
      - ./api:/app
    ports:
      - 3001:3001
    depends_on:
      - db
    env_file:
      - env_api.env

  front:
    build: ./front
    ports:
      - 3000:3000
    volumes:
      - ./front/:/app
    env_file:
      - env_front.env

  db:
    build: ./db
    volumes:
      - "./db/data/:/var/lib/postgresql/data"
    env_file:
      - env_db.env

  adminer:
    image: adminer
    ports:
      - 3002:8080
    depends_on:
      - db
